"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path"),crypto=require("node:crypto");
const directory=path.join(__dirname,"..","fixtures","ggg-build-v1"); const manifest=JSON.parse(fs.readFileSync(path.join(directory,"manifest.json"),"utf8"));
const canonicalFixtureBytes=bytes=>Buffer.from(bytes.toString("utf8").replace(/\r\n/g,"\n"),"utf8");
test("all 18 fixture hashes use the documented newline-canonical bytes",()=>{assert.equal(manifest.hashing,"sha256 UTF-8 with CRLF normalized to LF");assert.equal(manifest.fixtures.length,18);assert.equal(new Set(manifest.fixtures.map(x=>x.file)).size,18);for(const item of manifest.fixtures){assert.equal(crypto.createHash("sha256").update(canonicalFixtureBytes(fs.readFileSync(path.join(directory,item.file)))).digest("hex"),item.sha256);assert.ok(item.source);assert.ok(item.structuralExpectation);assert.equal(item.gameTest,"not tested");assert.equal(item.websiteUploadTest,"not tested");assert.ok(Array.isArray(item.unverified));}});
test("BOM and newline fixture bytes are explicit",()=>{const bom=fs.readFileSync(path.join(directory,"utf8-bom.build")), noBom=fs.readFileSync(path.join(directory,"utf8-no-bom-newline.build"));assert.deepEqual([...bom.slice(0,3)],[0xef,0xbb,0xbf]);assert.notDeepEqual([...noBom.slice(0,3)],[0xef,0xbb,0xbf]);assert.equal(noBom.at(-1),0x0a);});
