"use strict";
const test=require("node:test"), assert=require("node:assert/strict"); const {createPassiveIdMap}=require("../src/interop/passive-id-map.js");
test("maps explicit raw IDs without mutation and reports ambiguity",()=>{const nodes=Object.assign(Object.create(null),{"2491":{id:"jewel_slot1974"},"2":{id:"same"},"3":{id:"same"},"4":{},"__proto__":{id:"unsafe"}});const before=JSON.stringify(nodes);const map=createPassiveIdMap({nodes});assert.deepEqual(map.numericToOfficial(2491),{status:"mapped",numericId:"2491",officialId:"jewel_slot1974"});assert.equal(map.numericToOfficial("x").status,"invalid");assert.equal(map.numericToOfficial("4").status,"missing");assert.equal(map.officialToNumeric("nope").status,"missing");assert.deepEqual(map.officialToNumeric("same"),{status:"ambiguous",officialId:"same",numericIds:["2","3"]});assert.equal(JSON.stringify(nodes),before);assert.equal(map.numericToOfficial("__proto__").status,"invalid");});
test("is deterministic across key order and supports classifications",()=>{const a=createPassiveIdMap({nodes:{"2":{id:"b"},"1":{id:"a"}}},{classifyNumeric:id=>id==="1"?"normal":"special"}); const b=createPassiveIdMap({nodes:{"1":{id:"a"},"2":{id:"b"}}});assert.deepEqual(a.getMappingReport(),b.getMappingReport());assert.deepEqual(a.classifyNumeric(1),{status:"mapped",numericId:"1",classification:"normal"});assert.deepEqual(a.classifyNumeric("999"),{status:"missing",numericId:"999"});});
