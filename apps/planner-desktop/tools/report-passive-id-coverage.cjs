"use strict";
const crypto = require("node:crypto"), fs = require("node:fs"), path = require("node:path");
const { createPassiveIdMap } = require("../src/interop/passive-id-map.js");
function locked(lock, id) { const item = lock.sources.find(x => x.id === id); if (!item) throw Error(`lock ID missing: ${id}`); return item.integrity; }
function readVerified(file, integrity) { const bytes = fs.readFileSync(file); if (bytes.length !== integrity.bytes || crypto.createHash("sha256").update(bytes).digest("hex") !== integrity.sha256) throw Error(`canonical lock verification failed: ${file}`); return JSON.parse(bytes); }
function report(cacheDirectory, lockPath) {
 const lock = JSON.parse(fs.readFileSync(lockPath)); const official = readVerified(path.join(cacheDirectory,"official-data.json"), locked(lock,"shared.ggg.passive-tree")); const pre = readVerified(path.join(cacheDirectory,"tree-pre.json"), locked(lock,"runtime.drydream.tree-pre"));
 const map=createPassiveIdMap(official); const officialIds=Object.keys(official.nodes).sort(), preIds=Object.keys(pre.nodes).sort(), preSet=new Set(preIds), intersection=officialIds.filter(id=>preSet.has(id));
 const mapped=intersection.filter(id=>map.numericToOfficial(id).status==="mapped"); const conflicts=intersection.filter(id=>official.nodes[id]?.name !== pre.nodes[id]?.name);
 const ordinary=["2491","7960","21984","26196","26725","32763","46882","54127","55190","60735","61419","61834"];
 const ordinaryEvidence=ordinary.map(id=>({numericId:id,inOfficialJewelSlots:(official.jewelSlots||[]).map(String).includes(id),isJewelSocket:official.nodes[id]?.isJewelSocket===true}));
 const conflict11184={numericId:"11184",official:official.nodes["11184"]?.id,treePreName:pre.nodes["11184"]?.name,diagnostic:"official/tree-pre semantic conflict; exporter classification required"};
 const category=(ids,basis,status="verified",conflictIds=[])=>{const candidates=[...ids].sort();const mappedIds=candidates.filter(id=>map.numericToOfficial(id).status==="mapped");const missingIds=candidates.filter(id=>map.numericToOfficial(id).status==="missing");return {classificationStatus:status,classificationBasis:basis,candidateCount:candidates.length,mappedCount:mappedIds.length,missingCount:missingIds.length,ambiguousCount:0,conflictCount:conflictIds.length,coveragePercent:candidates.length?mappedIds.length/candidates.length*100:null,unmappedIds:missingIds,ambiguousIds:[],conflictIds};};
 const categories={normal:category([],"No explicit normal-node classifier is available in the locked inputs.","unverified"),ascendancy:category(["17788"],"Explicit accepted P2AT-008C ascendancy socket evidence."),weaponSet:category([],"Weapon-set is a Build passive-record property, not a static tree-node category; use reportAllocationCoverage for a supplied native allocation.","unverified"),instilledHidden:category([],"Instilled identity is canonical name, not numeric PassiveSkills.Id; hidden classification is not a locked tree category.","unverified"),ordinaryJewelSockets:category(ordinary,"Explicit accepted P2AT-008C ordinary socket ID list."),specialSockets:category(["17788","11184"],"Explicit accepted P2AT-008C special socket evidence.","verified",["11184"])};
 return { lockSchemaVersion:lock.schemaVersion, officialNodeCount:officialIds.length, treePreNodeCount:preIds.length, numericIntersection:intersection.length, mappedIntersection:mapped.length, treePreUnmapped:preIds.filter(id=>map.numericToOfficial(id).status!=="mapped").sort(), officialNotInTreePre:officialIds.filter(id=>!preSet.has(id)).sort(), mapping:map.getMappingReport(), semanticNameConflicts:conflicts.map(id=>({numericId:id,officialName:official.nodes[id]?.name,treePreName:pre.nodes[id]?.name})).sort((a,b)=>a.numericId < b.numericId ? -1 : a.numericId > b.numericId ? 1 : 0), categories, ordinaryJewelSockets:{ids:ordinary, evidence:ordinaryEvidence, mapped:ordinary.filter(id=>map.numericToOfficial(id).status==="mapped").length}, specialSocketIds:{ascendancy:["17788"],knownConflict:["11184"]}, conflict11184, knownMappings:["2491","7960","26725","17788","11184"].map(id=>map.numericToOfficial(id)) };
}
function reportAllocationCoverage(officialTree, numericIds) { const map=createPassiveIdMap(officialTree); const ids=Array.from(numericIds||[],String).sort(); const mapped=ids.filter(id=>map.numericToOfficial(id).status==="mapped"); return { classificationStatus:"provided-allocation", classificationBasis:"Caller-supplied native allocation IDs; appropriate for weaponSet1/weaponSet2 Build state.", candidateCount:ids.length, mappedCount:mapped.length, missingCount:ids.length-mapped.length, ambiguousCount:0, conflictCount:0, coveragePercent:ids.length?mapped.length/ids.length*100:null, unmappedIds:ids.filter(id=>map.numericToOfficial(id).status!=="mapped"), ambiguousIds:[], conflictIds:[] }; }
if(require.main===module) console.log(JSON.stringify(report(process.argv[2],process.argv[3]),null,2)); module.exports={report,reportAllocationCoverage};
